---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: conform-orchestrator
  namespace: conform
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  strategy:
    # The probe cache has one writer by design; a rolling update would briefly
    # give it two.
    type: Recreate
  selector:
    matchLabels:
      app: conform-orchestrator
  template:
    metadata:
      labels:
        app: conform-orchestrator
    spec:
      serviceAccountName: conform-orchestrator
      nodeSelector:
        node-type: standard
      # csi-driver-nfs provisions a volume root owned by root, and the image
      # runs as 3000 with no way to chown it itself.
      initContainers:
        - name: chown-state
          image: ghcr.io/edjeffreys/conform:sha-d5e365b@sha256:6a5485c86541bd695217f3551ae8ba3238cf328531aadf73c37a23b197b8fb2d
          # Recursive: the excuse ledger is a directory under /state, and a
          # chown of the mount point alone leaves it unwritable.
          command: ["chown", "-R", "3000:3000", "/state"]
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: state
              mountPath: /state
      containers:
        - name: conform
          image: ghcr.io/edjeffreys/conform:sha-d5e365b@sha256:6a5485c86541bd695217f3551ae8ba3238cf328531aadf73c37a23b197b8fb2d
          args:
            - orchestrate
            - -config
            - /config/conform.yaml
            - -verbose
            # Only has to notice files that arrive; the running queue refills
            # itself.
            - -interval
            - 1h
          ports:
            - name: webhook
              containerPort: 8080
          readinessProbe:
            tcpSocket:
              port: webhook
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 1Gi
              cpu: 1000m
          volumeMounts:
            - name: media
              mountPath: /data
              readOnly: true
            - name: state
              mountPath: /state
            - name: config
              mountPath: /config
      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: conform-media
        - name: state
          persistentVolumeClaim:
            claimName: conform-state
        - name: config
          configMap:
            name: conform-config
