// Module path anticipates the eventual standalone repo, so moving this
// directory out is a `git mv` with no import rewrites.
module github.com/edjeffreys/conform

go 1.25

require gopkg.in/yaml.v3 v3.0.1
